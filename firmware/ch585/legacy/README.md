# CH585 Legacy Source Snapshots

This directory keeps hand-written BLE/RF/USB experiment source for reference.
It is not a self-contained vendor SDK copy.

Shared WCH dependencies live under:

```text
firmware/ch585/basic/wch/SRC/
firmware/ch585/basic/wch/LIB/
```

Do not add MounRiver project metadata, generated build outputs, or duplicate
`Ld/`, `RVMSIS/`, `Startup/`, `StdPeriphDriver/`, or `LIB/` trees here.
